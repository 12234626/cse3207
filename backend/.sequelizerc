// Sequelize CLI 설정 파일

// 기본 경로
const base = "./build/db";

module.exports = {
  // Sequelize CLI 설정
  "config": `${base}/config/config.js`,
  // 모델 파일 경로
  "models-path": `${base}/models`,
  // 시더 파일 경로
  "seeders-path": `${base}/seeders`,
  // 마이그레이션 파일 경로
  "migrations-path": `${base}/migrations`
};
